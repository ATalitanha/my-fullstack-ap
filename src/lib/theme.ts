/**
 * A string of Tailwind CSS classes that define the application's theme.
 * @type {string}
 */
const theme =
	"bg-gradient-to-tr from-gray-100 via-gray-200 to-gray-300 dark:from-gray-900 dark:via-gray-800 dark:to-gray-950 transition-colors duration-500";

export default theme;
